# The deployment section should be excluded until
# it is explicitly included in module 5
# change added and one more
# additional test change
# modified the environment variable in CircleCI
#
deployment:
  hub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

test:
  override:    
    - mocha
